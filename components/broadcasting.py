from components.connection import send_message
from components.settings import group_members_ids


def broadcasting(user_id, message):
    pass
#     for member_id in group_members_ids:
#         send_message(member_id, message)
